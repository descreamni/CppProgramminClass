#include <iostream>
using namespace std;
int main() {
	/*
	cout << "립력";
	int n;
	cin >> n ;
	if (n <= 0)
		return 0;
	//n개의 정수 배열 동적 할당
	int* p = new int[n];
	if (!p) {
		cout << "메모리 할당 안되있다게이야";
		return 0 ;
	}
	for (int i = 0; i < n; i++)
	{
		cout << i + 1 << "번째 정수";
		cin >> p[i];
	}
	int sum = 0;
	for (int i = 0; i < n; i++)
	{
		sum += p[i];
	}
	cout << "합 : " << sum << endl;
	delete[] p;
	*/
	/*
	int* p;
	p = new int;
	if (!p) {
		cout << "메모리 할당 오류" << endl;
		return 0;
	}
	*p = 5;
	int n = *p;
	cout << "*p = " << *p << endl;
	cout << "*n = " << n << endl;
	delete p;
	*/
}